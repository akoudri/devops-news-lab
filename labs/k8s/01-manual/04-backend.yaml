apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
spec:
  replicas: 1 # On veut de la haute disponibilité !
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      enableServiceLinks: false
      containers:
        - name: api
          image: pueblo2708/devops-news-api:latest
          ports:
            - containerPort: 5000
          env:
            - name: REDIS_HOST
              value: "redis"
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: redis-secret
                  key: password
            - name: LOG_LEVEL
              valueFrom:
                configMapKeyRef:
                  name: backend-config
                  key: LOG_LEVEL
---
apiVersion: v1
kind: Service
metadata:
  name: backend
spec:
  type: ClusterIP # Accessible uniquement dans le cluster
  selector:
    app: backend
  ports:
    - port: 5000
      targetPort: 5000
